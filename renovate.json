{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "timezone": "Europe/London",
  "schedule": ["before 6am on monday"],
  "labels": ["dependencies", "agent-cli"],
  "customManagers": [
    {
      "description": "Track the pinned agent CLIs installed in the pod initContainers (#298). A broken upstream release must not reach the fleet on the next pod start; Renovate opens a bump PR the cli-canary gates.",
      "customType": "regex",
      "fileMatch": [
        "^apps/(aifactory|pfactory|tfactory)/manifests/manifests\\.yaml$"
      ],
      "matchStrings": [
        "(?<depName>@anthropic-ai/claude-code|@openai/codex|@google/gemini-cli)@(?<currentValue>\\d+\\.\\d+\\.\\d+)"
      ],
      "datasourceTemplate": "npm"
    }
  ],
  "packageRules": [
    {
      "description": "One grouped weekly PR for all three agent CLIs; never auto-merge — the cli-canary must pass and a human confirms.",
      "matchDepNames": [
        "@anthropic-ai/claude-code",
        "@openai/codex",
        "@google/gemini-cli"
      ],
      "groupName": "agent CLIs",
      "automerge": false
    }
  ]
}
