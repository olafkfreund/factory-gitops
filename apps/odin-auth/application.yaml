# Odin SSO — oauth2-proxy in front of odin-dashboard.
# Source: apps/odin-auth/manifests in this repo.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: odin-auth
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/olafkfreund/factory-gitops
    targetRevision: main
    path: apps/odin-auth/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: factory
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
