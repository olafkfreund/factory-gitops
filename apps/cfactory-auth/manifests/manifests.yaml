# Keycloak SSO for CFactory via oauth2-proxy.
#
# CFactory's frontend (cfactory-frontend:80) has no native auth; this
# fronts it with oauth2-proxy talking OIDC to keycloak.factory.svc:8080
# (realm `factory`, client `cfactory`). The cluster cloudflared ingress
# for cfactory.freundcloud.org.uk points HERE instead of the frontend,
# so every public request is gated by a Keycloak login before reaching
# the cockpit UI. The cfactory backend (cfactory:3111) stays internal —
# only the frontend talks to it (BACKEND_URL=...svc.cluster.local:3111).
#
# Convention note: same pattern as apps/odin-auth/. aifactory/pfactory/
# tfactory still use native in-app OIDC (APP_OIDC_* env vars).
#
# Secret `oauth2-proxy-cfactory` (client-id, client-secret, cookie-secret)
# is created out-of-band via kubectl — NOT in this repo until
# sealed-secrets / external-secrets-operator lands.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy-cfactory
  namespace: factory
  labels:
    app: oauth2-proxy-cfactory
    app.kubernetes.io/instance: cfactory-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oauth2-proxy-cfactory
  template:
    metadata:
      labels:
        app: oauth2-proxy-cfactory
    spec:
      enableServiceLinks: false
      # Factory#521. This pod had NO securityContext at all — and it is the
      # front door to the cockpit: every public request to
      # cfactory.freundcloud.org.uk is terminated here before it reaches the UI.
      #
      # 65532 is NUMERIC on purpose and it is what the image already declares
      # (measured: docker image inspect -> Config.User "65532"), so nothing
      # changes at runtime. A NAME cannot be resolved by runAsNonRoot alone —
      # AIFactory#848 killed every build Job that way.
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.7.0
          # readOnlyRootFilesystem is MEASURED, not assumed. This exact tag run
          # read-only as 65532 with --cap-drop ALL and no-new-privileges comes up
          # and answers GET /ping -> 200, which is the readiness/liveness path
          # below — so the check is a request against the port, not a pod status
          # (the cfactory-frontend trap in #108: nginx stayed Running with no
          # server block and refused every connection).
          # Every input is a read-only mount: the alpha config, and the
          # client-secret file. Sessions are cookie-backed, not disk-backed —
          # there is no Redis session store configured — so nothing is written.
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          args:
            # Provider, upstream and header injection live in the alpha config
            # (see alpha-config.yaml) — required for the tenant claim ->
            # X-Tenant-Id header (#13). That includes the editor OIDC layer:
            # --skip-jwt-bearer-tokens accepts a valid Keycloak JWT bearer (from
            # the factory-vscode public client, which carries `cfactory` in its
            # audience via a Keycloak audience mapper) instead of forcing an
            # interactive browser login; the extra audience itself is
            # oidcConfig.extraAudiences in the alpha config.
            - --alpha-config=/etc/oauth2-proxy/config/oauth2-proxy.yaml
            - --redirect-url=https://cfactory.freundcloud.org.uk/oauth2/callback
            - --reverse-proxy=true
            - --email-domain=*
            - --cookie-secure=true
            - --cookie-samesite=lax
            - --skip-provider-button=true
            - --whitelist-domain=cfactory.freundcloud.org.uk
            - --skip-jwt-bearer-tokens=true
          env:
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-cfactory
                  key: cookie-secret
          volumeMounts:
            - name: alpha-config
              mountPath: /etc/oauth2-proxy/config
              readOnly: true
            # client-secret consumed as a file (alpha config clientSecretFile);
            # same out-of-band `oauth2-proxy-cfactory` Secret as before.
            - name: oidc-secrets
              mountPath: /etc/oauth2-proxy/secrets
              readOnly: true
          ports:
            - name: http
              containerPort: 4180
          readinessProbe:
            httpGet:
              path: /ping
              port: 4180
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /ping
              port: 4180
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: alpha-config
          configMap:
            name: oauth2-proxy-cfactory-alpha
        - name: oidc-secrets
          secret:
            secretName: oauth2-proxy-cfactory
            items:
              - key: client-secret
                path: client-secret
---
apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy-cfactory
  namespace: factory
  labels:
    app: oauth2-proxy-cfactory
    app.kubernetes.io/instance: cfactory-auth
spec:
  selector:
    app: oauth2-proxy-cfactory
  ports:
    - name: http
      port: 4180
      targetPort: 4180
