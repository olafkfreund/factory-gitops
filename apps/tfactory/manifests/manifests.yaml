# THE LIVE data PVC again as of 2026-08-24 — the rollback this comment was
# written for. The NFS export backing tfactory-data-rwx cannot serve git:
# `git init` on the mount fails `Invalid path ...: Remote I/O error` while plain
# mkdir/write/read on the same mount succeed, and the same git command works on
# the pod's ephemeral disk, so it is the export and not git, the image or
# permissions. Every verify stage failed `source_checkout_failed` because the
# clone could not create `.git/objects/pack`.
#
# The #781 hazard this was migrated away from -- local-path RWO pinning the
# volume to one node, so a cross-node roll stranded specs -- does not apply
# today: the cluster is a single node running a single tfactory replica. If a
# second node returns, this has to move back to RWX, which means fixing the NFS
# export first rather than re-pointing at it.
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: tfactory-data, namespace: factory, labels: { app: tfactory } }
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources: { requests: { storage: 5Gi } }
---
# PARKED 2026-08-24: kept so the data survives and so re-pointing here is one
# line once the export is fixed. Was the live volume until its NFS backing
# stopped serving git (see above).
# Workspace data on RWX NFS (#781) — any node's control-plane pod OR verify
# Job can mount it, so a cross-node roll no longer strands specs. Migrated from
# tfactory-data 2026-07-23 (spec state copied; git clones re-materialize via the
# #539 self-heal). Provisioned by apps/nfs-provisioner.
apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: tfactory-data-rwx, namespace: factory, labels: { app: tfactory } }
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: nfs
  resources: { requests: { storage: 20Gi } }
---
# The warm /nix/store PVC (RFC-0016 #197) is GONE — removed with its GC CronJob
# and TFACTORY_NIX_STORE_PVC env (TFactory#623/#665, mirrors AIFactory's
# factory-gitops#60).
#
# Its own comment carried the assumption that broke it: "RWO is fine on this
# single-node cluster". The cluster has had two nodes since 2026-06-26. RWO means
# only ONE pod mounts it at a time, so the evaluator's concurrent per-test /
# stability-sample / mutation Jobs SERIALISED on a single mount — the fleet's
# 3-4 concurrent ceiling was this lock, not a resource limit, which is why 80
# idle cores never helped. It also explained #623's intermittency: a Job that won
# the mount race found the closure prebuilt, one that lost got a cold /nix.
#
# It was a latent hard failure too: local-path is WaitForFirstConsumer, so each PV
# lands wherever its first consumer did. tfactory-data and tfactory-nix-store
# happened to land on the same node — luck, not design. AIFactory's split across
# nodes and every pod mounting both RWO claims became unschedulable outright
# (Factory#253, AIFactory#830).
#
# Nothing needs it: the tfactory-runner-nix image bakes the very store the PVC was
# seeded from (build_job_manifest's seed initContainer copied the image's own /nix
# INTO it), and TFACTORY_NIX_IN_IMAGE=true makes both the control plane and the
# dispatched Job resolve /nix from the image. Cost: closures realised during a
# task are re-fetched per Job instead of persisting — speed for concurrency.
---
# RFC-0005 Tier A: SA + least-privilege RBAC so TFactory can dispatch ephemeral
# Nix-lane Jobs (tools/runners/kube_sandbox.py) and read their logs. Scoped to
# the factory namespace; jobs + pod logs only.
apiVersion: v1
kind: ServiceAccount
metadata: { name: tfactory-sandbox, namespace: factory, labels: { app: tfactory } }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata: { name: tfactory-sandbox, namespace: factory, labels: { app: tfactory } }
rules:
  - { apiGroups: ["batch"], resources: ["jobs"], verbs: ["create","get","list","watch","delete"] }
  - { apiGroups: [""], resources: ["pods"], verbs: ["get","list"] }
  - { apiGroups: [""], resources: ["pods/log"], verbs: ["get"] }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata: { name: tfactory-sandbox, namespace: factory, labels: { app: tfactory } }
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: Role, name: tfactory-sandbox }
subjects:
  - { kind: ServiceAccount, name: tfactory-sandbox, namespace: factory }
---
# Deploy-lane dry-run SA (#603 gap 2) — the SERVICE ACCOUNT the deploy verify Job
# runs as for `kubectl create --dry-run=server` (VAL-2). Named to the control
# plane via TFACTORY_DEPLOY_DRYRUN_SA (below). Scoped to namespaced resources —
# cluster-scoped kinds stay an honest not_run.
#
# Factory#569 — WHY `create` IS THE ONLY VERB. This SA's token is mounted into
# the one Job in this cluster that executes MODEL-GENERATED content (the deploy
# descriptors, plus `tofu init`/`plan`, which runs provider code). Until #569 the
# Role carried `get`/`list` on `secrets` with no resourceNames, so anything that
# ran in that Job could read `factory-secrets`, the `factory-db-*` DATABASE_URLs,
# `minio-creds` and the oauth2-proxy client secrets. `assert_dry_run` does not
# mitigate that — it constrains writes, and this was a read.
#
# The verbs below are MEASURED against the live k3d API server by impersonating
# this SA, not assumed:
#   * `kubectl apply --dry-run=server` GETs every object first to compute its
#     merge patch, so it needs `get` on EVERY kind in the descriptor — including
#     `secrets`. Denying `get secrets` under `apply` breaks the rung with
#     "error when retrieving current configuration of ... Kind=Secret".
#     That is why the obvious narrowing (drop secrets, keep create) does NOT work
#     on its own, and why TFactory moved the rung to `create` first.
#   * `kubectl create --dry-run=server` POSTs straight to the API with
#     dryRun=All and needs `create` ALONE. Measured green on a Secret, ConfigMap,
#     Deployment, Service, Job and Ingress descriptor with an identity holding no
#     read verb whatsoever.
#   * `list` was never exercised by either spelling — `apply -f`/`create -f` GET
#     or POST by name and never enumerate.
# So: no `get`, no `list`, on any kind. This token can now read NOTHING in the
# namespace. `create` still authorizes dryRun=All; a real write is blocked by
# deploy_runner.assert_dry_run (which, as of #569, also refuses a bare
# `kubectl create` — dropping the flag is no longer a silent real apply).
#
# Factory#570 — `create` IS NOT HARMLESS, AND assert_dry_run IS NOT WHAT BOUNDS
# IT. That guard is client-side, in the SAME Job as the model-generated content
# it polices, and RBAC cannot tell a dryRun=All create from a real one. So
# `create pods`/`jobs`/`deployments` here is a privilege-escalation primitive: a
# REAL pod naming `serviceAccountName: cred-broker` (or tfactory-sandbox) hands
# the caller that SA's token; a real ServiceAccount + RoleBinding mints a fresh
# identity. Demonstrated on the live cluster. The layer that expresses the
# constraint RBAC cannot is the Kyverno ClusterPolicy
# `deploy-dryrun-sa-dryrun-only` (apps/kyverno-policies/manifests, ENFORCE),
# which denies any NON-dryRun create by this SA while letting
# `create --dry-run=server` through. Keep the two together: narrowing these
# verbs further breaks the lane (create is required), so the admission policy —
# not this Role — is what makes the grant safe.
#
# ORDERING: this Role REQUIRES the TFactory image to be running the `create`
# spelling (TFactory#953). It ships only once that image is deployed; landing it
# against an `apply` build breaks the VAL-2 rung on every kind, not just Secrets.
apiVersion: v1
kind: ServiceAccount
metadata: { name: tfactory-deploy-dryrun, namespace: factory, labels: { app: tfactory } }
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata: { name: tfactory-deploy-dryrun, namespace: factory, labels: { app: tfactory } }
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "configmaps", "secrets", "serviceaccounts", "persistentvolumeclaims"]
    verbs: ["create"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets", "statefulsets", "daemonsets"]
    verbs: ["create"]
  - apiGroups: ["batch"]
    resources: ["jobs", "cronjobs"]
    verbs: ["create"]
  - apiGroups: ["networking.k8s.io"]
    resources: ["ingresses", "networkpolicies"]
    verbs: ["create"]
  - apiGroups: ["autoscaling"]
    resources: ["horizontalpodautoscalers"]
    verbs: ["create"]
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["roles", "rolebindings"]
    verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata: { name: tfactory-deploy-dryrun, namespace: factory, labels: { app: tfactory } }
roleRef: { apiGroup: rbac.authorization.k8s.io, kind: Role, name: tfactory-deploy-dryrun }
subjects:
  - { kind: ServiceAccount, name: tfactory-deploy-dryrun, namespace: factory }
---
apiVersion: v1
kind: Service
metadata: { name: tfactory, namespace: factory, labels: { app: tfactory } }
spec:
  selector: { app: tfactory }
  ports: [{ name: http, port: 3103, targetPort: 3103 }]
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: tfactory, namespace: factory, labels: { app: tfactory } }
spec:
  # No `replicas:` — KEDA's HPA owns the replica count (RFC-0016 #192). ArgoCD
  # ignores /spec/replicas (see application.yaml ignoreDifferences) so selfHeal
  # does not fight the HPA. RollingUpdate (not Recreate) so KEDA can run >1 pod;
  # the RWO data PVC co-mounts on the single k3d node (multi-replica-safe now
  # that authoritative state is in Postgres, #220).
  selector: { matchLabels: { app: tfactory } }
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: 1, maxUnavailable: 0 } }
  template:
    metadata: { labels: { app: tfactory } }
    spec:
      # RFC-0005 Tier A: this SA can create/read the ephemeral Nix-lane Jobs.
      serviceAccountName: tfactory-sandbox
      # Factory#550. This pod DOES need its SA token, and says so rather than
      # inheriting the default. tools/runners/kube_sandbox.py calls
      # `kubernetes_asyncio.config.load_incluster_config()` to create every
      # Nix-lane verify Job, and core/mcp_credentials.py reads
      # /var/run/secrets/kubernetes.io/serviceaccount/token directly. Verified in
      # the running image: kubernetes_asyncio is installed at
      # .venv/lib/python3.14/site-packages. Setting this false stops verification.
      #
      # The chart already agreed in substance (templates/deployment.yaml renders
      # `automountServiceAccountToken: {{ .Values.rbac.jobSandbox.enabled }}`,
      # which is true) while values.yaml still declared `false` on the
      # ServiceAccount. Factory#504's gate now compares the value, so that
      # internal contradiction cannot come back.
      automountServiceAccountToken: true
      imagePullSecrets: [{ name: ghcr-pull }]
      enableServiceLinks: false
      # Factory#503. Every CronJob in this namespace has carried this set since
      # it was written; the Deployments never did, so nothing VERIFIED that the
      # control plane runs non-root — the image's `USER nonroot` was the only
      # thing holding it, and an image change that dropped it would start this
      # pod as root silently.
      # runAsUser is explicit and NUMERIC on purpose: the chainguard image's
      # `USER nonroot` is a NAME, and `runAsNonRoot: true` alone cannot resolve a
      # name — that is AIFactory#848, where every build Job died
      # CreateContainerConfigError. 65532 IS chainguard's `nonroot`, i.e. the uid
      # this pod already runs as; setting it changes nothing at runtime and makes
      # the kubelet refuse to start the pod the day the image changes.
      securityContext:
        fsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile: { type: RuntimeDefault }
      initContainers:
        # The provider CLIs (claude-code/codex/gemini + antigravity) are now BAKED
        # into the image at /home/nonroot/.npm-global (TFactory #791) — the
        # install-clis init container that npm-installed them at boot hung 8+ min
        # on a slow registry and stalled the whole rollout. Versions are pinned +
        # Renovate-tracked in the Dockerfile; a bump is an image rebuild + canary.
        # Seed per-CLI OAuth credential files into writable dirs (so the CLIs can refresh tokens).
        - name: seed-creds
          image: busybox:1.36@sha256:73aaf090f3d85aa34ee199857f03fa3a95c8ede2ffd4cc2cdb5b94e566b11662
          command: ["sh","-c"]
          args:
            - |
              set -e
              mkdir -p /h/.claude /h/.codex /h/.gemini /h/.config/github-copilot
              cp /seed/claude-credentials.json  /h/.claude/.credentials.json
              cp /seed/codex-auth.json          /h/.codex/auth.json
              cp /seed/copilot-apps.json        /h/.config/github-copilot/apps.json
              cp /seed/gemini-oauth_creds.json  /h/.gemini/oauth_creds.json
              # `|| true` is load-bearing under the pod-level runAsUser: 65532
              # added for Factory#503. The four mount ROOTS are owned root:65532
              # (kubelet applies fsGroup), and chmod(2) requires ownership, so as
              # uid 65532 they return EPERM. Measured, not assumed:
              #   chmod: /h/.claude: Operation not permitted
              # With `set -e` that aborts the init container and the pod never
              # starts at all. Everything BELOW the roots still gets chmod'd.
              # The chmod is near-redundant now anyway — every file here is
              # created by 65532, so it is already owner-writable.
              chmod -R g+rwX /h/.claude /h/.codex /h/.gemini /h/.config || true
              echo "seeded:"; ls -la /h/.claude /h/.codex /h/.gemini
          # Writes only into the mounts above, so a read-only root fs is free.
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - { name: cc-claude, mountPath: /h/.claude }
            - { name: cc-codex,  mountPath: /h/.codex }
            - { name: cc-gemini, mountPath: /h/.gemini }
            - { name: cc-config, mountPath: /h/.config }
            - { name: cli-creds, mountPath: /seed, readOnly: true }
      containers:
        # Credential sync (Factory#292). seed-creds copies the Secret into the
        # writable ~/.claude emptyDir ONCE at pod start, so a credential the
        # cred-broker CronJob refreshes never reaches a long-running pod (the
        # pod keeps a stale/expired token until it happens to restart). The
        # cli-creds Secret VOLUME is auto-refreshed by kubelet, so this sidecar
        # just re-copies it in whenever the seed is newer than what's on disk.
        # "Newer only" matters: the agent SDK also writes this file when it
        # refreshes, and we must not clobber a fresher token with a staler one.
        - name: cred-sync
          image: python:3.12-slim@sha256:646fb0bca3dd3ea1bcc6feb72c17ed16eed6e10cffc732fcc1478bd3e7f02d7b
          command: ["python3", "-u", "-c"]
          args:
            - |
              # cred-sync (Factory#292, hardened by Factory#628). The cli-creds Secret
              # volume is refreshed by kubelet, so this re-copies the seed into the writable
              # ~/.claude emptyDir whenever the seed is genuinely newer than what is there.
              #
              # Factory#628: exp() used to collapse every read failure -- missing file,
              # malformed JSON, absent key -- to 0, and 0 is not a sentinel here, it is the
              # oldest possible timestamp. An unreadable destination therefore lost every
              # comparison, so ANY seed was copied over it, including one that had expired
              # two months earlier, announced as "adopted fresher credential (+494672.6h)".
              # 494672.6h is 56 years: it is seed_expiry_ms/3600000 with the other side at 0.
              #
              # Tested by .github/scripts/test_cred_sync.py, which also proves the three
              # vendored copies of this script are byte-identical.
              import json, os, shutil, time

              SEED = os.environ.get("CRED_SYNC_SEED", "/seed/claude-credentials.json")
              DEST = os.environ.get("CRED_SYNC_DEST", "/h/.claude/.credentials.json")
              # No real credential is 90 days fresher than the one it replaces. A delta that
              # large means one side is garbage, not that the seed is better.
              MAX_SKEW_MS = 90 * 86400000


              def exp(path):
                  """Expiry in ms, or None when unreadable / unparseable / no expiry key.

                  None means "unknown" and is deliberately not a number. Substituting a
                  timestamp for a read failure is the whole of Factory#628.
                  """
                  try:
                      with open(path) as fh:
                          value = json.load(fh).get("claudeAiOauth", {}).get("expiresAt")
                  except Exception:
                      return None
                  return None if value is None else int(value)


              def ts(ms):
                  if ms is None:
                      return "unknown"
                  return time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime(ms / 1000))


              def decide(seed, dest, dest_exists, now):
                  """(adopt, message). Pure, so the selftest can pin every branch."""
                  if seed is None:
                      return False, "seed unreadable, leaving dest untouched"
                  if seed <= now:
                      return False, f"seed expires {ts(seed)}, already past (now {ts(now)}), refusing to adopt"
                  if dest is None and dest_exists:
                      return False, "dest exists but is unreadable, refusing to overwrite it"
                  # `dest > 0`: a literal 0 expiry is a CLEARED credential, not a read
                  # failure. exp() returns None for unreadable/unparseable/absent-key --
                  # that was the whole of Factory#628 -- so 0 can now only come from a
                  # real `expiresAt: 0` in valid JSON, which is what the Claude SDK
                  # leaves behind when it clears an access token.
                  #
                  # Without this the bound wedges the pod permanently: dest reads 0, the
                  # delta is ~56 years, cred-sync refuses forever, and every run dies at
                  # the planner with "No OAuth token found" while a valid seed sits in
                  # the Secret (observed 2026-08-27, TFactory spec 191).
                  if dest is not None and dest > 0 and seed - dest > MAX_SKEW_MS:
                      return False, (f"seed expires {ts(seed)}, dest expires {ts(dest)}, delta "
                                     f"{(seed - dest) / 3600000:.1f}h exceeds the sanity bound, refusing")
                  if dest is not None and seed <= dest:
                      return False, ""
                  # States what was compared, not a conclusion: this line cannot be wrong the
                  # way "adopted fresher credential" was.
                  return True, f"seed expires {ts(seed)}, dest expires {ts(dest)}, adopting seed"


              def sync(now=None):
                  now = int(time.time() * 1000) if now is None else now
                  adopt, why = decide(exp(SEED), exp(DEST), os.path.exists(DEST), now)
                  if adopt:
                      tmp = DEST + ".tmp"
                      shutil.copyfile(SEED, tmp)
                      os.chmod(tmp, 0o660)
                      os.replace(tmp, DEST)  # atomic; readers never see a partial file
                  return adopt, why


              def main():
                  last = None
                  while True:
                      try:
                          why = sync()[1]
                      except Exception as e:
                          why = f"{type(e).__name__}: {e}"
                      if why and why != last:  # a refusal recurs every 60s; say it once
                          print(f"cred-sync: {why}", flush=True)
                      last = why
                      time.sleep(60)


              if __name__ == "__main__":
                  main()
          securityContext:
            allowPrivilegeEscalation: false
            # Stdlib-only inline script; every write goes to the /h/.claude
            # emptyDir. Verified read-only + uid 65532 against python:3.12-slim.
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests: { cpu: 5m, memory: 24Mi }
            limits: { cpu: 100m, memory: 64Mi }
          volumeMounts:
            - { name: cc-claude, mountPath: /h/.claude }
            - { name: cli-creds, mountPath: /seed, readOnly: true }
        - name: tfactory
          image: ghcr.io/olafkfreund/tfactory:0.6.4
          imagePullPolicy: IfNotPresent
          # Factory#520. readOnlyRootFilesystem is now ON. #503 left it off with
          # an honest reason; this is the enumeration that reason asked for,
          # measured by running THIS image read-only rather than by reading the
          # source. Identical shape to aifactory and pfactory — see
          # apps/aifactory/manifests/manifests.yaml for the full write-path
          # table; the three images share a base and a HOME layout.
          #
          # The obvious fix stays wrong: /home/nonroot/.npm-global is ~1.1G of
          # baked provider CLIs (#791), so an emptyDir over /home/nonroot would
          # MASK them. Every volume below therefore sits BELOW /home/nonroot.
          #
          # Measured, this exact image, read-only + uid 65532 + cap-drop ALL +
          # no-new-privileges: alembic migrations ran to head,
          # "Application startup complete", uvicorn on :3103,
          # GET /api/health -> 200.
          #
          # NOTE: that same run surfaced a PRE-EXISTING defect unrelated to this
          # change — a missing `import sys` makes both the #774 startup
          # reconcile and the #742 worktree GC fail on every boot, swallowed by
          # `(continuing)`. Reproduced on the live pod too. TFactory#918.
          #
          # What is NOT proven here: a full verify run. A write failure
          # mid-verify is a runtime 500, not a CrashLoop, so pod status will not
          # catch it — watch one real verification after this rolls out.
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports: [{ containerPort: 3103 }]
          env:
            - { name: APP_HOST, value: "0.0.0.0" }
            - { name: APP_PORT, value: "3103" }
            # Liveness sweep (#95): flip a silent in-process stage (planning /
            # generating / evaluating) to `stalled` after the deadline, so a hung
            # gen_functional/evaluator LLM session (updated_at frozen) does not
            # strand the spec as "running" forever. Was OFF by default; a live gcd
            # spec hung 23min in gen_functional with no safety net (2026-07-22).
            - { name: APP_LIVENESS_SWEEP_ENABLED, value: "true" }
            # RFC-0016 #220/#468: durable shared state in Postgres (tfactory DB).
            # Replaces pod-local SQLite (~/.tfactory/data.db); init_db() runs
            # `alembic upgrade head` on boot (MIGRATIONS_AUTO_APPLY default true),
            # creating job_states + all tables. Makes verify-job state durable +
            # multi-replica safe. Out-of-band secret factory-db-tfactory.
            - { name: DATABASE_URL, valueFrom: { secretKeyRef: { name: factory-db-tfactory, key: DATABASE_URL } } }
            # RFC-0016 #190: S3-compatible object store (in-cluster MinIO) for
            # verify artifacts (findings/verdicts.json, test reports, evidence).
            # The shared client (apps/backend/tools/runners/artifact_store.py)
            # reads these; emission is fail-open so an outage never blocks a
            # verdict. Creds from the out-of-band `minio-creds` Secret.
            - { name: S3_ENDPOINT, value: "http://minio.factory.svc.cluster.local:9000" }
            - { name: S3_BUCKET, value: "factory-artifacts" }
            - { name: S3_ACCESS_KEY, valueFrom: { secretKeyRef: { name: minio-creds, key: S3_ACCESS_KEY } } }
            - { name: S3_SECRET_KEY, valueFrom: { secretKeyRef: { name: minio-creds, key: S3_SECRET_KEY } } }
            # bwrap can't run on k3d (node is a container) — disable the bash sandbox
            # so agent bash works. Real syscall sandbox via gVisor: AIFactory #363.
            - { name: AIFACTORY_BASH_SANDBOX, value: "false" }
            - { name: PATH, value: "/home/nonroot/.npm-global/bin:/home/projects/MagesticAI/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" }
            # Factory#520. What makes readOnlyRootFilesystem possible at all:
            # the claude CLI writes ~/.claude.json at the HOME ROOT, a single
            # FILE that no emptyDir can cover. This relocates it into the
            # .claude emptyDir that already exists — the same directory
            # seed-creds seeds, so credential resolution is unchanged.
            - { name: CLAUDE_CONFIG_DIR, value: "/home/nonroot/.claude" }
            # The web-server process must import the backend's `agents` package
            # (create_spec_ingest_workspace schedules agents.planner on ingest).
            # Without apps/backend on the path the import fails silently and the
            # ingested spec sits at status=pending — the verify pipeline never
            # runs. (#71: AIFactory→TFactory handoff lands but TFactory no-ops.)
            - { name: PYTHONPATH, value: "/home/projects/MagesticAI/apps/backend" }
            - { name: TFACTORY_COMPLETION_WEBHOOK, value: "http://cfactory.factory.svc.cluster.local:3111/api/events" }
            # Auto-run the verify pipeline on spec ingest (planner → gen-functional
            # → evaluate → triage) so an AIFactory→TFactory handoff actually
            # generates + runs tests and emits a verdict. Without these the spec
            # ingests but sits at status=pending (the PARR verify leg never runs).
            - { name: TFACTORY_AUTO_PLAN, value: "true" }
            - { name: TFACTORY_AUTO_GENERATE, value: "true" }
            - { name: TFACTORY_AUTO_EVALUATE, value: "true" }
            - { name: TFACTORY_AUTO_TRIAGE, value: "true" }
            # Surface the VAL verdict on the PR (#964): commit the triager's
            # accepted+flagged tests back onto the AIFactory feature branch so
            # a green verify visibly adds its verified tests to the open PR.
            # Default is dry-run; this flag makes the write live. (git_writer
            # resolves the handoff's source_branch as of TFactory#716.)
            - { name: TFACTORY_TRIAGER_GIT_WRITE, value: "1" }
            # Post the triage report as a PR comment (#964). AIFactory attaches
            # the PR number after it opens (POST /api/specs/{p}/{s}/pr); the
            # triager then comments the verdict. Default is dry-run.
            - { name: TFACTORY_TRIAGER_PR_COMMENT, value: "1" }
            # VAL-3 disposable-target provisioner (TFactory#607, Factory#257):
            # verify runs whose val block requires a disposable target get a
            # real throwaway k8s Job. Lazily registered at the
            # disposable_target() choke point when this flag is set; inert for
            # specs without a disposable-target requirement. Job is torn down
            # on all paths, no credentials in Job env/argv.
            - { name: TFACTORY_VAL3_K8S_JOB, value: "1" }
            # Pinned :sha-<short>@sha256:<digest>, not :latest (TFactory#890).
            # The DIGEST is what resolves and it cannot move -- which matters
            # more now the Kyverno signature gate is at Enforce (Factory#522),
            # because a tag can be repointed at something unsigned between one
            # admission check and the next. The TAG rides along as readable
            # provenance ("which commit produced this verdict"), which the Nix
            # lane -- the DEFAULT verify execution path -- needs to be able to
            # answer. nix-runner-image.yml rewrites both lines on every publish
            # and FAILS if either is absent, rather than sed-ing a silent no-op
            # (the #719 failure mode). Do not hand-edit.
            - { name: TFACTORY_VAL3_K8S_JOB_IMAGE, value: "ghcr.io/olafkfreund/tfactory-runner-nix:sha-ec0819e@sha256:2d6887f19967472b4d1b52cf20be64c54dde868928d778a8df1954329e681921" }
            # RFC-0005 Tier A / RFC-0016 #469: the Nix-lane k8s-Job sandbox
            # co-mounts the worktree from this PVC and runs the per-task flake in
            # the thin Nix image. nixjob is now the DEFAULT verify execution path
            # (the code prefers it when TFACTORY_NIX_RUNNER_IMAGE is set and the
            # contract environment is a nix env; catalog-image/host-venv remain
            # fallbacks).
            #
            # TFACTORY_NIX_STORE_PVC is deliberately UNSET (#623): /nix comes from
            # the runner image, not a node-pinned RWO warm store. Unset means
            # nix_runner_from_env passes nix_store_pvc=None, which kube_sandbox
            # documents as "no mount" — the cold-fetch path. TFACTORY_NIX_IN_IMAGE
            # keeps it dropped even if the var is ever reintroduced, and MUST reach
            # the dispatched Job: nix_runner_from_env runs again in there, so a flag
            # that does not travel reads False and re-mounts the PVC (verify_dispatch
            # propagates it; that half-applied shape is how AIFactory#840 broke its
            # gate lane). Do not reintroduce the PVC without RWX storage.
            - { name: TFACTORY_WORKSPACES_PVC, value: "tfactory-data" }
            # #603 gap 2: names the SA the deploy Job runs as for kubectl
            # --dry-run=server (VAL-2). Set => the kubectl rung runs; unset =>
            # honest not_run. RBAC = the tfactory-deploy-dryrun SA/Role above.
            - { name: TFACTORY_DEPLOY_DRYRUN_SA, value: "tfactory-deploy-dryrun" }
            - { name: TFACTORY_NIX_RUNNER_IMAGE, value: "ghcr.io/olafkfreund/tfactory-runner-nix:sha-ec0819e@sha256:2d6887f19967472b4d1b52cf20be64c54dde868928d778a8df1954329e681921" }
            - { name: TFACTORY_NIX_IN_IMAGE, value: "true" }
            # TFACTORY_IMAGE pins the TFactory backend image the verify Job runs
            # on (the image that now ships the `agents` package on PYTHONPATH —
            # the ModuleNotFoundError that reverted FLIP #3 was fixed in #479).
            # CD bumps the kustomization newTag but NOT this env — keep this SHA
            # aligned with kustomization.yaml newTag (FLAT manifests; no Helm
            # image-injection). 2026-06-24: realigned sha-e5f5b01 (2026-06-21,
            # pre-#544) -> sha-cbc593a (== kustomization newTag). The stale pin
            # meant the verify Job ran an evaluator WITHOUT the Go test-lane
            # dispatch (#544 `_completed_go_subtasks` / run_gotest_lane_via_nix),
            # so a Go verify found 0 completed subtasks -> empty verdicts ->
            # triaged_empty. cbc593a carries #544 + the spec-ingest delete fix
            # (#546). 2026-06-24 re-aligned cbc593a -> sha-62e2535 (== newTag) to
            # pick up #547 (share one Go module stability across its subtasks: the
            # 12-Jobs-per-module path let a transient dispatch error flip stability
            # to ERROR, downgrading accept->flag). Keep in lockstep with newTag on
            # every CD bump.
            - { name: TFACTORY_IMAGE, value: "ghcr.io/olafkfreund/tfactory:sha-68554db" }
            # PORTAL_UI_IMAGE pins the runner image the portal-ui browser Job
            # runs (portal_testing/dispatch.py). TFactory#886: nothing built that
            # image for the first five weeks of its life, so `:latest` -- the
            # default when this is unset -- sat on a hand-run 27 June build while
            # #875/#876/#877 were merged and green. The lane in the cluster kept
            # running the June harness, which set the Job POD's labels to
            # `app: tfactory` (the `tfactory` Service selector), joined the
            # Service, answered its share of real portal traffic with connection
            # refused, and served Cloudflare 502s for the duration of every test
            # run -- then filed the portal it had just taken down as broken.
            #
            # A stale floating tag is indistinguishable from a current one, which
            # is exactly how that went unnoticed. So this is an IMMUTABLE
            # `sha-<short>` tag, bumped by TFactory's
            # .github/workflows/portal-ui-runner-image.yml on every main push
            # that touches portal_testing/** or the runner's Dockerfile. That
            # workflow FAILS if this line is absent, rather than sed-ing nothing
            # and pushing a silent no-op (the #719 failure mode).
            #
            # is rebuilt only when its inputs change, so it lags the backend tag
            # and that is correct. It must always name a commit whose
            # portal_testing/ is the one intended to run.
            - { name: PORTAL_UI_IMAGE, value: "ghcr.io/olafkfreund/tfactory-runner-portal-ui:sha-c827deb@sha256:1e821225203976c907e579bbaf17411f466fc3da2d990562f074d2b67689238c" }
            # RFC-0016 #466 / RFC-0017: Job-native verify orchestration.
            # FLIP #1 (2026-06-21) REVERTED — verify_dispatch was present but NOT
            # WIRED (nothing called verify_exec_mode), so the flip was inert.
            # #476 (live as sha-081cc6d) FIXED the wiring, AND IT WORKS: with
            # TFACTORY_VERIFY_EXEC=kubejob the verify now DISPATCHES a real k8s Job
            # (factory-tfactory-erify-<spec>) with the correct nix-develop-wrapped
            # entrypoint `nix develop path:/work#default --command python -m
            # agents.verify_pipeline …` on TFACTORY_NIX_RUNNER_IMAGE. So the wiring
            # defect #466 fixed is PROVEN resolved (verified live on aifactory-test,
            # spec verify-kubejob-035558).
            # FLIP #2 (2026-06-21, post-#476) REVERTED — two NEW downstream defects
            # (NOT the wiring, NOT the pre-existing review-lane stall — the pipeline
            # got all the way past review to dispatch):
            #   (a) verify_dispatch._record_dispatch raised RuntimeError "got Future
            #       attached to a different loop" — _run_dispatch_blocking runs the
            #       async job_state_store.enqueue on its own event loop while the
            #       store's asyncpg/SQLAlchemy session is bound to the main loop. The
            #       dispatch record FAILED (logged WARNING + continued), so no durable
            #       job_states row exists → reconcile can never produce a verdict.
            #   (b) the verify Job pod failed exit=1: `flake.nix does not exist` at
            #       /work — the per-task Nix flake is not materialized into the
            #       co-mounted worktree PVC, so `nix develop path:/work#default`
            #       fails before verify_pipeline runs.
            # Net: Job dispatched but no terminal verdict reached job_states
            # (status/verdict=None). Needs CODE fixes in TFactory (run the enqueue on
            # the store's loop; materialize the task flake into /work for the Job).
            # FIX (#478, merged a82c340): (a) verify_dispatch._record_dispatch builds
            # a fresh async engine bound to the dispatch's own loop (no cross-loop
            # Future); (b) the per-task flake is materialized into the project
            # worktree (flake_subpath) before dispatch so `nix develop path:/work`
            # resolves.
            # FLIP #3 (2026-06-21, post-#478) REVERTED — but #478 DID work, BOTH
            # fixes PROVEN live on aifactory-test (Job
            # factory-tfactory-fy-kubejob-d2-044024):
            #   (a) a durable job_states row LANDED for the verify job
            #       (kind=verify, worker_ref recorded; NO "Future attached to a
            #       different loop" error), and the control-plane reconcile PICKED
            #       IT UP (lifecycle_state transitioned to a terminal `stuck` with
            #       an honest "no verdict" error). Cross-loop store defect: FIXED.
            #   (b) the per-task flake MATERIALIZED into /work — the entrypoint
            #       `nix develop path:/work/workspaces/olafkfreund-aifactory-test
            #       #default` SUCCEEDED (whole nix env incl. pytest/fastapi fetched
            #       from cache, python3-3.13 env built). NO "flake.nix does not
            #       exist". Missing-flake defect: FIXED.
            # A NEW, distinct next-in-chain defect surfaced: after nix develop
            # succeeded, the Job's `python -m agents.verify_pipeline …` died with
            # `ModuleNotFoundError: No module named 'agents'`. The nix runner image
            # (TFACTORY_NIX_RUNNER_IMAGE) + per-task flake provide the TOOLCHAIN
            # (python/pytest/deps) but NOT TFactory's own backend `agents` package,
            # and it isn't on PYTHONPATH from /work (which is the SUT workspace, not
            # the TFactory backend). So verify_pipeline never runs evaluate→triage,
            # the Job produces no verdict, and reconcile honestly marks the row
            # `stuck` ("verify Job finished … with no verdict — lanes pending #464").
            # Needs a CODE fix in TFactory dispatch (ship/mount the backend `agents`
            # package into the verify Job + set PYTHONPATH, or build the runner image
            # to contain it). Cannot be fixed from gitops env. Staying on the in-pod
            # verify default until that lands.
            # FLIP #4 (round-4, 2026-06-21, post-#479) REVERTED — but #479 DID
            # work, the ModuleNotFoundError that reverted FLIP #3 is RESOLVED.
            # Proven live on aifactory-test, spec verify-kubejob-d4-130020, Job
            # factory-tfactory-fy-kubejob-d4-130020 (image sha-fe2cb3b):
            #   - generation was slow (plan→generate→replan loop; both test
            #     subtasks hit replan_count=2), but it DID complete and the verify
            #     DISPATCHED a real k8s Job (not the pre-dispatch stall).
            #   - the verify Job had PYTHONPATH set and TFactory's `agents`
            #     package IMPORTED — execution reached deep into
            #     agents.evaluator.run_evaluator → _run_evaluator_session →
            #     create_client. NO `ModuleNotFoundError: No module named
            #     'agents'`. #479 (agents-on-PYTHONPATH) is PROVEN resolved.
            # A NEW, distinct next-in-chain defect surfaced — the SAME one Stage B
            # hit on the build Job: the verify Job is created with only
            # JOB_ID/FACTORY_SERVICE/DATABASE_URL/PYTHONPATH — it does NOT
            # propagate the LLM credential (CLAUDE_CODE_OAUTH_TOKEN). So the
            # evaluator died `ValueError: No OAuth token found … TFactory requires
            # Claude Code OAuth authentication` (core/auth.py require_auth_token);
            # no verdict reached job_states; Job terminal Failed
            # (BackoffLimitExceeded). (S3_ENDPOINT unset → artifact upload skipped,
            # verdict-unaffected — incidental.)
            # Needs a CODE fix in TFactory verify dispatch (verify_dispatch /
            # gen_functional kubejob Job-spec): inject CLAUDE_CODE_OAUTH_TOKEN into
            # the verify Job container, mirroring the in-pod env. This is the same
            # class of fix the build Job needs (AIFactory #671 FLIP #4). Cannot be
            # fixed from gitops. Back to in-pod verify default (safe).
            # FLIP #5 (round-5, 2026-06-23) — ENABLED. The FLIP #4 blocker (verify
            # Job missing CLAUDE_CODE_OAUTH_TOKEN → core/auth.require_auth_token
            # ValueError) is RESOLVED on the current runtime image. Proof: a
            # side-process dispatch_verify_job smoke (job_id verifysmoke466, verify
            # image sha-e5f5b01) ran the FULL pipeline in a real k8s Job —
            # `agents` imported (no ModuleNotFoundError), the evaluator ran the
            # generated pytest (stability 3/3, mutation killed Eq→NotEq), and the
            # triager executed a full LLM agent session (prompt-cache
            # read=46225/write=25147 tok — the exact credential path that failed
            # FLIP #4), returned verdict=accept, and wrote a durable job_states row
            # lifecycle_state=done / service_status=triaged with a traceability
            # matrix (tests/test_smoke.py::u1, VAL-1, passed). #481 CLI-creds seed
            # (below) adds file-auth provider parity. TFactory #466.
            - { name: TFACTORY_VERIFY_EXEC, value: "kubejob" }
            # #481: file-auth CLI creds for the dispatched verify Job — seeds the
            # factory-cli-creds secret into the Job pod via the seed-creds
            # initContainer (verify_dispatch._inject_verify_seed_creds). INERT
            # until TFACTORY_VERIFY_EXEC=kubejob (default in-pod), so it is safe to
            # set ahead of the verify-default flip: the Job then has file-auth
            # provider parity (codex/gemini-oauth/copilot) once kubejob is enabled.
            - { name: TFACTORY_VERIFY_CLI_CREDS_SECRET, value: "factory-cli-creds" }
            # Opt-in LLM staff-engineer review lane (additive, parallel, never
            # feeds the verdict path). Writes findings/review.json each verify.
            - { name: TFACTORY_REVIEW_LANE, value: "1" }
            - { name: GITHUB_TOKEN, valueFrom: { secretKeyRef: { name: factory-secrets, key: GITHUB_TOKEN } } }
            # Shared static API token for programmatic access (CFactory's adapters
            # send it as `Authorization: Bearer`). Matches aifactory so all three
            # factories accept the one shared token from factory-secrets.
            - { name: APP_API_TOKEN, valueFrom: { secretKeyRef: { name: factory-secrets, key: APP_API_TOKEN } } }
            # Federated cross-portal search (#149): read-scoped cockpit key so this
            # portal's /api/search proxy can query the cockpit's aggregated index.
            - { name: CFACTORY_READ_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: CFACTORY_READ_KEY } } }
            # OIDC SSO via Keycloak
            - { name: APP_OIDC_ENABLED, value: "true" }
            - { name: APP_OIDC_ISSUER_URL, value: "https://keycloak.freundcloud.org.uk/realms/factory" }
            - { name: APP_OIDC_CLIENT_ID, value: "tfactory" }
            - { name: APP_OIDC_CLIENT_SECRET, valueFrom: { secretKeyRef: { name: factory-secrets, key: TFACTORY_OIDC_CLIENT_SECRET } } }
            - { name: APP_OIDC_REDIRECT_URI, value: "https://tfactory.freundcloud.org.uk/api/auth/oidc/callback" }
            # provider keys — power the claude / codex / antigravity CLIs seeded above
            - { name: OPENAI_API_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: OPENAI_API_KEY } } }
            - { name: GEMINI_API_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: GEMINI_API_KEY } } }
            # Headless/automated runs: the gemini/antigravity CLI refuses to run in
            # an "untrusted" workspace and exits BEFORE any API call, so a Gemini
            # verify run fails with no request ever leaving the pod. AIFactory hit
            # exactly this on 2026-06-13 and fixed it the same way; TFactory can
            # invoke Gemini (providers/gemini_agentic.py) and was still missing it.
            # TFactory#871.
            - { name: GEMINI_CLI_TRUST_WORKSPACE, value: "true" }
            # Self-hosted Ollama for the native (non-openai-compatible) route. The
            # provider class defaults to http://localhost:11434 and reads no env of
            # its own, so without this every ollama-routed phase talks to a port
            # nothing listens on inside the pod. TFactory#870 resolves this var in
            # phase_config.get_provider_extra_kwargs and forwards it to the verify
            # Job. Same address as OPENAI_COMPATIBLE_BASE_URL below — note that name
            # currently does NOT resolve from any pod (factory-gitops#196); fixing
            # that record fixes both routes at once.
            - { name: OLLAMA_BASE_URL, value: "http://host.k3d.internal:11434" }
            # Ollama (openai-compatible) so the verify lanes (planner/evaluator/qa)
            # can run on openai-compatible:<model> models routed via the handoff
            # contract — same config AIFactory uses. Without the base URL the
            # provider defaults to api.openai.com and the planner session fails
            # fast (missing/None → planner_failed) for an Ollama-routed verify.
            # Pointed at Ollama CLOUD, not the p510 daemon. Loading a large
            # model locally took the host down twice on 2026-08-20 (a 17.7GB
            # qwen3.8:27b run), and the cloud endpoint is OpenAI-compatible so
            # the same `openai-compatible:<model>` strings route there unchanged
            # -- no provider change (TFactory#306). It DOES require a real API
            # key, unlike the local daemon.
            #
            # Free tier reaches 7 of the 19 models it lists: gpt-oss:120b,
            # gpt-oss:20b, gemma4:31b, minimax-m3, nemotron-3-nano:30b,
            # nemotron-3-super, nemotron-3-ultra. The rest (qwen3.5:397b,
            # kimi-k2.7-code, kimi-k3, deepseek-v4-*, glm-5.*,
            # mistral-large-3:675b, minimax-m2.7) answer HTTP 403. Probe with a
            # real completion before pinning one -- listing is not access.
            - { name: OPENAI_COMPATIBLE_BASE_URL, value: "https://ollama.com" }
            - { name: OPENAI_COMPATIBLE_API_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: OPENAI_COMPATIBLE_API_KEY } } }
            # Bound local-model thinking + reserve output budget (verify Planner
            # else returns finish=length empty on big prompts). Forwarded to the
            # verify Job by verify_dispatch._SDK_PASSTHROUGH_ENV.
            - { name: OPENAI_COMPATIBLE_REASONING_EFFORT, value: "low" }
            - { name: OPENAI_COMPATIBLE_MAX_TOKENS, value: "32768" }
            - { name: CONTEXT7_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: CONTEXT7_KEY } } }
            - { name: LANGCHAIN_API_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: LANGCHAIN_API_KEY } } }
            - { name: RAPIDAPI_KEY, valueFrom: { secretKeyRef: { name: factory-secrets, key: RAPIDAPI_KEY } } }
            - { name: CLAUDE_CODE_OAUTH_TOKEN, valueFrom: { secretKeyRef: { name: factory-secrets, key: CLAUDE_CODE_OAUTH_TOKEN } } }
            # OpenTelemetry export -> in-cluster OpenObserve (apps/observe,
            # service `observe:5080`). Factory#516: until now `grep -c
            # OTEL_EXPORTER` across the pfactory, tfactory and cfactory
            # Deployments returned 0 — they were not misconfigured, they were
            # unconfigured, so they logged no export failure and their silence
            # read as health. A PARR run crosses PFactory -> AIFactory ->
            # TFactory; with three of four untraced a trace could never span a
            # handoff, which is the one question distributed tracing exists to
            # answer.
            #
            # OpenObserve ingests OTLP/HTTP under an org-scoped base and the SDK
            # appends /v1/{traces,metrics,logs}; org = `default`.
            #
            # The credential is the SAME derived Secret AIFactory reads —
            # `otel-otlp-auth`, reconciled hourly from `observe-root` by
            # apps/observe's observe-otlp-auth-sync — not a fourth hand-copied
            # copy. That reconciler exists because the hand-maintained version
            # drifted to Bearer, which OpenObserve v0.90.3 answers with `401 Not
            # Supported`; it authenticates ingest with HTTP Basic and nothing
            # else (Factory#465).
            #
            # optional=true: the Secret is created by that CronJob, so on a fresh
            # cluster the pod must still start before the first reconcile. An
            # absent header means no auth header, which the app's own startup
            # probe reports as "NO SPANS WILL LAND" rather than claiming success.
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT, value: "http://observe.factory.svc.cluster.local:5080/api/default" }
            - { name: OTEL_EXPORTER_OTLP_PROTOCOL, value: "http/protobuf" }
            - { name: OTEL_EXPORTER_OTLP_HEADERS, valueFrom: { secretKeyRef: { name: otel-otlp-auth, key: headers, optional: true } } }
          volumeMounts:
            - { name: data,      mountPath: /home/nonroot/.tfactory }
            - { name: cc-claude, mountPath: /home/nonroot/.claude }
            - { name: cc-codex,  mountPath: /home/nonroot/.codex }
            - { name: cc-gemini, mountPath: /home/nonroot/.gemini }
            - { name: cc-config, mountPath: /home/nonroot/.config }
            # Factory#520. All BELOW /home/nonroot on purpose, so the 1.1G of
            # baked CLIs at .npm-global stays visible.
            - { name: tmp,        mountPath: /tmp }
            - { name: home-cache, mountPath: /home/nonroot/.cache }
            - { name: home-npm,   mountPath: /home/nonroot/.npm }
            - { name: home-local, mountPath: /home/nonroot/.local }
          readinessProbe: { httpGet: { path: /api/health, port: 3103 }, initialDelaySeconds: 10, periodSeconds: 10, failureThreshold: 6 }
          livenessProbe:  { httpGet: { path: /api/health, port: 3103 }, initialDelaySeconds: 30, periodSeconds: 20 }
          resources: { requests: { cpu: 100m, memory: 1Gi }, limits: { memory: 8Gi } }
      volumes:
        - { name: data,      persistentVolumeClaim: { claimName: tfactory-data } }
        - { name: cc-claude, emptyDir: {} }
        - { name: cc-codex,  emptyDir: {} }
        - { name: cc-gemini, emptyDir: {} }
        - { name: cc-config, emptyDir: {} }
        - { name: cli-creds, secret: { secretName: factory-cli-creds } }
        # Factory#520 — the writable holes a read-only rootfs needs.
        - { name: tmp,        emptyDir: {} }
        - { name: home-cache, emptyDir: {} }
        - { name: home-npm,   emptyDir: {} }
        - { name: home-local, emptyDir: {} }
---
# The tfactory-nix-store-gc CronJob (RFC-0016 #197) is GONE with the PVC it
# trimmed (#623). It nightly `nix store gc`'d a warm store that nothing writes to
# any more — garbage-collecting garbage. Per-Job /nix now comes from the image and
# dies with the Job, so there is nothing to cap. Its aifactory twin went the same
# way in factory-gitops#60.
