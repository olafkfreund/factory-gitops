apiVersion: v1
kind: Namespace
metadata:
  name: argocd
  # The bootstrap unit on p510 will, after this is applied, create a
  # `tailscale-auth-key` Secret in this namespace (key TS_AUTHKEY) for
  # the sidecar to consume.
  labels:
    app.kubernetes.io/part-of: argocd
